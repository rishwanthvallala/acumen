/** @type {import('next').NextConfig} */
const nextConfig = {
   images: {
       domains: ['images.unsplash.com', 'aceternity.com'],
       // Define remotePatterns as an array of strings
   },
};

export default nextConfig;
